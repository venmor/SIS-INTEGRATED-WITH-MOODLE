# The active GitHub Actions workflow is ../../../../.github/workflows/ci.yml.
# GitHub only discovers .github/workflows at the repository root.
# This historical location is retained as a pointer, not a second workflow.
