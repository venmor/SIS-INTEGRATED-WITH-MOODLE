# Config

Typed, versioned institutional configuration (policy values with effective dates). Policy lives here as data — never hard-coded in controllers or components. Populated from the first policy-driven slice.
